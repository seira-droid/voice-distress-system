[pytest]
DJANGO_SETTINGS_MODULE = config.settings
python_files = tests.py test_*.py *_tests.py

addopts = --cov=distress_app --cov-report=term-missing

# IMPORTANT: DO NOT disable project discovery
django_find_project = true

# ensures Django root is importable
pythonpath = .
